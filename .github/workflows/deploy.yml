name: Deploy to Local Docker

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t my-node-backend .

      - name: Stop and Remove Old Container (if exists)
        continue-on-error: true
        run: |
          docker stop node-backend-container
          docker rm node-backend-container

      - name: Run New Docker Container
        # Notice the -e flag below! 
        # This injects the GitHub secret into your Docker container as an environment variable.
        run: |
          docker run -d -p 3000:3000 \
          -e DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
          --name node-backend-container my-node-backend
